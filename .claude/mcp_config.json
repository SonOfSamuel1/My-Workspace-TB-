{
  "mcpServers": {
    "ynab": {
      "command": "node",
      "args": [
        "${workspaceFolder}/servers/ynab-mcp-server/dist/index.js"
      ],
      "env": {
        "YNAB_API_KEY": "${env:YNAB_API_KEY}"
      }
    }
  }
}
