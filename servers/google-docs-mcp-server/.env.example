# Google Docs MCP Server Configuration
# Copy this file to .env and update the paths if needed

# Optional: Custom path to your Google OAuth2 credentials
# If not set, looks for credentials.json in the server directory
# GOOGLE_CREDENTIALS_PATH=/path/to/credentials.json

# Optional: Custom path to your Google OAuth2 token
# If not set, looks for token.json in the server directory
# GOOGLE_TOKEN_PATH=/path/to/token.json
