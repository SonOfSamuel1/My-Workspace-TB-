# JT Teaching Newsletter — Environment Variables
# Copy this file to .env and fill in your values
# NEVER commit the actual .env file

# =============================================================================
# REQUIRED
# =============================================================================

# API key for Claude (reflection question generation)
# Option A: Native Anthropic key (sk-ant-...)
ANTHROPIC_API_KEY=sk-ant-...

# Option B: OpenRouter key — also set ANTHROPIC_BASE_URL + CLAUDE_MODEL below
# ANTHROPIC_API_KEY=sk-or-v1-...
# ANTHROPIC_BASE_URL=https://openrouter.ai/api/v1
# CLAUDE_MODEL=anthropic/claude-haiku-3-5

# S3 bucket containing JT- teaching .md files + state.json
JT_S3_BUCKET=jt-teachings-notes

# Email address to receive daily teachings
JT_EMAIL_RECIPIENT=your@email.com

# =============================================================================
# AWS SES
# =============================================================================

# Verified sender email in AWS SES
SES_SENDER_EMAIL=brandonhome.appdev@gmail.com

# AWS region (default: us-east-1)
AWS_REGION=us-east-1

# =============================================================================
# AWS CREDENTIALS (local dev only — Lambda uses IAM role)
# =============================================================================
# These are needed locally if you don't use aws configure / ~/.aws/credentials
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=
