# YNAB Transaction Reviewer Environment Variables

# Required: Your YNAB Personal Access Token
# Get it from: https://app.ynab.com/settings/developer
YNAB_API_KEY=your-ynab-personal-access-token

# Optional: Specific budget ID (defaults to first budget)
# Find it in YNAB URL: https://app.ynab.com/[BUDGET_ID]/budget
YNAB_BUDGET_ID=

# Required: Email address to receive daily reviews
RECIPIENT_EMAIL=your-email@gmail.com

# Custom Web App for Deep Linking (transaction review URLs in emails)
# Deploy the web/ directory to Vercel and set this to your deployment URL
YNAB_WEB_APP_URL=https://your-app.vercel.app

# Optional: Override configuration file path
CONFIG_PATH=config/config.yaml

# Optional: Enable debug logging
DEBUG=false

# AWS deployment (only needed for Lambda)
AWS_REGION=us-east-1
AWS_LAMBDA_FUNCTION=ynab-transaction-reviewer