# Atlanta News Report Configuration
# Copy this file to .env and fill in your values

# Email recipient for the weekly news report
ATLANTA_NEWS_EMAIL=your_email@example.com

# AWS SES Configuration
SES_SENDER_EMAIL=brandonhome.appdev@gmail.com
AWS_REGION=us-east-1

# Optional: AWS credentials (not needed if using IAM role or AWS CLI configured)
# AWS_ACCESS_KEY_ID=your_access_key
# AWS_SECRET_ACCESS_KEY=your_secret_key

# Environment (development, staging, production)
ENVIRONMENT=development

# Lambda mode flag (set automatically in Lambda)
# IS_LAMBDA=false
