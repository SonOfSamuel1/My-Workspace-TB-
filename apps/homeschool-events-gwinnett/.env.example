# OpenRouter API (optional - only needed if Perplexity source is enabled in config.yaml)
# OPENROUTER_API_KEY=sk-or-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Email Configuration
EVENTS_EMAIL_RECIPIENT=your-email@example.com
SES_SENDER_EMAIL=brandonhome.appdev@gmail.com

# AWS Configuration
AWS_REGION=us-east-1

# Optional: AWS credentials (not needed if using IAM role or ~/.aws/credentials)
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=

# Environment
ENVIRONMENT=development
IS_LAMBDA=false
