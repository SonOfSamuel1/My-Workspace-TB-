# Factor 75 Credentials
FACTOR75_EMAIL=your-factor75-email@example.com
FACTOR75_PASSWORD=your-factor75-password

# User Email (where to send meal selection emails)
USER_EMAIL=your-email@example.com

# AWS SES Configuration
AWS_REGION=us-east-1
SES_SENDER_EMAIL=noreply@yourdomain.com

# Gmail API (for reply polling)
# Place credentials.json in credentials/ directory
GMAIL_CREDENTIALS_PATH=credentials/credentials.json
GMAIL_TOKEN_PATH=credentials/token.pickle

# Optional: AWS Parameter Store prefix (for Lambda deployment)
# AWS_PARAMETER_PREFIX=/factor75-selector
