# OneNote to Obsidian Migration - Environment Variables
# ======================================================
# Copy this file to .env and fill in your values

# Azure AD Application Credentials
# --------------------------------
# Required: Application (client) ID from Azure AD app registration
AZURE_CLIENT_ID=your-azure-client-id-here

# Optional: Tenant ID (use 'common' for personal Microsoft accounts)
# For work/school accounts, use your organization's tenant ID
AZURE_TENANT_ID=common

# Optional: Client secret (only needed for app-only authentication)
# For personal migration, device code flow is recommended (no secret needed)
# AZURE_CLIENT_SECRET=your-client-secret-here

# Optional: Custom path for token cache
# AZURE_TOKEN_CACHE=./credentials/token_cache.json

# Optional: Custom vault path (overrides config.yaml)
# OBSIDIAN_VAULT_PATH=/path/to/your/vault
